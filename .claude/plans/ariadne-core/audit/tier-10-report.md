---
tier_id: tier-10
audited: 2026-05-20
verdict: FAIL
commit: 2de7c0ba7c1f084b53ef8f5c1285a9ed42db6e80
---

<scope>
Audit of `tier-10-cli-e2e.md` — CLI surface + end-to-end fixture suite + SLO
release gate. The tier carries `status: blocked` (not `completed`); the
spec-audit gate normally refuses a non-`completed` tier. This audit runs on
explicit user override ("Full formal audit"), with the user pre-informed the
verdict would be FAIL.

Scoped diff: the tier's `<files>` plus build-created siblings — `crates/
ariadne-cli/**` (Cargo.toml, main.rs, config.rs, domain/mod.rs, commands/*.rs,
errors.rs), `crates/ariadne-e2e/**` (Cargo.toml, src/{lib,errors,domain}.rs,
tests/{slo,mcp_session}.rs, tests/repos/*.rs, fixtures/repos.toml),
`.github/workflows/release.yml`, `dist-workspace.toml`, `Cargo.toml`,
`tests/architecture.rs`, `docs/folder-layout.md`, `docs/adr/0007-…md`,
`docs/codebase-overview.md`, `README.md`. 1798 insertions / 103 deletions.
</scope>

<checks_run>
Re-run in-session:
- `cargo fmt --all --check` — PASS (exit 0).
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` — PASS (exit 0).
- `cargo test --test architecture` — PASS (1 test ok; ADR-0007 split holds).
- `cargo nextest run --workspace` — PASS (125 passed, 7 skipped — the 6 `#[ignore]`d repo suites + `slo`).
- `cargo build -p ariadne-cli` + `ariadne --help` — PASS (7 subcommands: init, index, watch, serve, query, status, mem).
- CLI smoke on a 2-file temp repo: `init` (config.toml + langs + 4/7 indexers), `index` (`{"files":2,"symbols":4,"edges":3,…}`), `status` (counts + indexer matrix), `query blast_radius` (real output), `ARIADNE_ENABLED_LANGS` override — all functional. `mem` — see F4.

Not re-run (resource-prohibitive, multi-GB OSS clones / >10 min):
- `slo.rs` release gate and the `#[ignore]`d per-lang repo suites. Verdict does
  not depend on re-running them — F2/F3/F4 are independently verified, and the
  implementer's own evidence (tier-10 `<blockers>`) records the SLO failure.
- `dist build` 5-target cross-build (CI-only; host-only build reported green).

exit_criteria verification:
- #1 (7-subcommand binary) — MET (verified via `--help` + smoke).
- #2 (`.ariadne/config.toml` + `ARIADNE_*` overrides) — MET (verified).
- #3 (≥100K-file corpus, cold <60s, incr/query p95) — NOT MET → F1.
- #4 (real-repo per-lang SCIP insta goldens) — NOT MET → F2.
- #5 (`ariadne ingest-scip` walk + Go path) — `ingest-scip` removed (D-E,
  accepted); Go SCIP path externally blocked — `lsif-go` cannot build
  (upstream `sourcegraph/sourcegraph` + `slimsag/godocmd` deleted). This is
  risk R3 materialising; mitigation already in plan. Go still indexes
  syntactically. Recorded here per the exit_criterion's "documented in audit".
- #6 (all MCP tools E2E + insta goldens) — PARTIAL → F3.
- #7 (cargo-dist 5 targets) — config correct (`dist-workspace.toml` lists the
  5 triples; `release.yml` regenerated by dist 0.31, D-F); full 5-target build
  unverifiable in-session.
- #8 (README install + mcp.json + Go hint) — MET.

External behaviour cited: cargo-dist 0.31 moved config from
`[workspace.metadata.dist]` to `dist-workspace.toml` [src: https://opensource.axo.dev/cargo-dist/] — D-F is correct. redb `WriteTxn::apply` consumes `self` and calls `txn.commit()` internally [src: crates/ariadne-storage/src/adapters/redb/mod.rs:139-146] — `run_index`'s `drop(storage)` after `apply` is sound.
</checks_run>

<findings>
| id | category | severity | location | problem | fix |
|---|---|---|---|---|---|
| F1 | exit_criteria / performance | FAIL | `crates/ariadne-e2e/tests/slo.rs:62-79`; `crates/ariadne-cli/src/domain/mod.rs:140-191` | exit_criteria #3 unmet: corpus reaches only 55,527 indexed files (<100K floor) and cold index 442.8s (>60s SLO); incr+query p95 unverified (the test panics at the cold stage). The single-threaded read-all-into-RAM `walk_repo`/`materialise` loop also holds every file's bytes resident — unreviewed against the 4GB ceiling. Risk R8. | Land tier-12 (parallelise walk→parse→materialise, move SCIP off the cold path) and tier-11 (C/C++ so the corpus reaches a real 100K), then re-run the gate. |
| F2 | exit_criteria / tests | FAIL | `crates/ariadne-e2e/` (whole crate) | exit_criteria #4 / step 10a entirely unimplemented: no real-repo per-lang SCIP golden test, no `insta` dependency in `ariadne-e2e/Cargo.toml`, no snapshot of `IngestReport` + symbol/relationship counts + top-5 symbols. The tier-05 deferral is not discharged. | Execute step 10a — add the SCIP-golden suite (one ≤100-LOC fixture per lang, real `ScipIndexer::run`, insta snapshots) on the 7-indexer CI image. |
| F3 | exit_criteria / tests | FAIL | `crates/ariadne-e2e/tests/mcp_session.rs:97-110` | exit_criteria #6 partially unmet: all 13 tools are exercised via the spawned client, but the test asserts only "no error frame" + p95 latency — there is no `insta` golden of any tool output, so "insta golden outputs stable" is not delivered. | Snapshot each tool's `content[0].text` payload with `insta` so output drift is caught. |
| F4 | correctness | FAIL | `crates/ariadne-cli/src/commands/mem.rs:13-39` | `ariadne mem` builds `AriadneDb::new()` — a fresh empty in-RAM DB — instead of the persisted index (`AriadneDb` exposes only `new()`; no load-from-storage — `crates/ariadne-salsa/src/db.rs:45`). Smoke-tested: against a populated index every table reports `0` bytes and the command always exits "ok". It can never flag an over-budget table — its sole purpose per step 9 and risk R1. | Seed `AriadneDb` from `.ariadne/index.redb` (or report redb-backed sizes) before `memory_report()`; if no persisted-salsa path exists, escalate as a plan decision. |
| I1 | architecture | INFO | `crates/ariadne-cli/src/commands/watch.rs:46`; `serve.rs:44` | `watch` and `serve --watch` drive a fresh `AriadneDb::new()` neither seeded from nor flushed back to `.ariadne/index.redb`, so neither maintains the persisted index; the apply latencies `watch` logs (consumed by `slo.rs measure_incremental`) measure empty derived state. Plan dataflow "watcher → … → writes deltas to redb" is unrealised. Non-blocking only because the incremental SLO is already deferred (R9). | Wire the watcher to the persisted index in tier-12 alongside the incremental-SLO work. |
| I2 | performance | INFO | `crates/ariadne-cli/src/domain/mod.rs:123` | `index` runs `IngestPlan::ingest` (all SCIP indexers as subprocesses) after the redb commit and uses the result only for the JSON summary's `scip_*` fields — the `ScipDoc` output is discarded (D-A stub bridge). On `dotnet/runtime` this spends ~354s in `scip-dotnet` for thrown-away output, directly inflating the cold-index breach. | tier-12 makes SCIP opt-in off the measured cold path — consistent with this finding. |
| I3 | plan_adherence | INFO | `crates/ariadne-cli/src/domain/mod.rs`; `crates/ariadne-e2e/src/{lib,errors,domain/mod}.rs` | The cold-index engine and the e2e harness (~945 lines) landed in crate `src/` files the tier's `<files>` list does not name. Benign — shared `[[test]]` helpers and the bin crate's orchestration must live in lib/bin modules, not in test files — but the `<files>` list was incomplete. | None required; note the `<files>` omission for future tier authoring. |
</findings>

<verdict>
FAIL — 4 FAIL findings, 3 INFO.

This is the expected and correct outcome. tier-10 is `status: blocked` by its
own design: the `<verification>` rule mandates "if ANY SLO fails, v1 does not
ship — open a follow-up tier". The implementer did exactly that (tiers 11, 12
created; risks R8/R9 added). The CLI surface, config layer, MCP session test,
release config, README, and ADR-0007 are sound, build clean, and pass clippy /
fmt / nextest / the architecture invariant. But four exit_criteria are unmet
(#3, #4) or only partial (#5, #6), and `ariadne mem` ships non-functional.

Accepted deviations D-A..D-G were reviewed and are not re-litigated; D-E
(`ingest-scip` folded into `index`) and D-F (`dist-workspace.toml`) are
verified correct. ADR-0007 (composition root) properly records the
`tests/architecture.rs` rule-4 amendment and the test passes.

The audit-gate hook will read this FAIL verdict and block `git commit` /
`git push` — as the user was informed when authorising the formal audit. To
land the tier-10 code before the SLO work, the verdict must become PASS, which
requires the redo steps below; alternatively delete `audit-state.json` to
return the gate to neutral.
</verdict>

<next_steps>
Tier steps to redo (in a fresh `/spec-build` session, after tiers 11–12):
- Step 1 / exit_criteria #3 (F1): re-run the SLO gate once tier-12 parallelises
  the cold-index loop and tier-11 adds C/C++; corpus must reach ≥100K indexed
  files and cold <60s, then incremental + query p95 must be verified.
- Step 10a / exit_criteria #4 (F2): build the real-repo per-lang SCIP golden
  suite with `insta`; add `insta` to `ariadne-e2e/Cargo.toml`.
- Step 11 / exit_criteria #6 (F3): add `insta` golden snapshots of MCP tool
  outputs to `mcp_session.rs`.
- Step 9 (F4): make `ariadne mem` report the real index, not an empty DB.
- Steps 6/5 (I1): connect `watch` / `serve --watch` to the persisted index.
Do not silence `slo.rs`; do not mark tier-10 `completed` until the gate is green.
</next_steps>

<sources>
- [src: .claude/plans/ariadne-core/tier-10-cli-e2e.md] — exit_criteria, steps, `<blockers>`, deviations D-A..D-G.
- [src: .claude/plans/ariadne-core/plan.md] — `<constraints>` SLOs, risks R1/R3/R8/R9, tiers 11/12.
- [src: crates/ariadne-storage/src/adapters/redb/mod.rs:139-146] — `WriteTxn::apply` commit semantics.
- [src: crates/ariadne-salsa/src/db.rs:45] — `AriadneDb` exposes only `new()`.
- [src: docs/adr/0007-cli-composition-root.md] — composition-root carve-out.
- [https://opensource.axo.dev/cargo-dist/] — cargo-dist 0.31 `dist-workspace.toml`.
- [https://google.github.io/eng-practices/review/reviewer/standard.html] — reviewer standard.
</sources>
</content>
</invoke>
