import React from 'react';

// .jsx extension — same grammar path as .tsx
export const LegacyButton = ({ label }) => <button style={{ padding: '8px' }}>{label}</button>;

export const LegacyIcon = () => <><span>icon</span></>;
