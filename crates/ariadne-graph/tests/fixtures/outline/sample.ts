/**
 * A documented exported function.
 * Adds two numbers.
 */
export function add(a: number, b: number): number {
    const sum = a + b;
    return sum;
}

interface Hidden {
    id: number;
}

/** A small exported constant. */
export const TWO = 2;
